# Listen to all available interfaces
bind 0.0.0.0
# No restrict on Valkey from listening to interfaces other than localhost
protected-mode no
port 6379
maxmemory 64mb
maxmemory-policy allkeys-lru
aclfile /etc/valkey/users.acl
dir /data
