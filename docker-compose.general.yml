# networks:
#     guacnetwork_compose:
#         driver: bridge

# services:
    # watchtower:
    #     image: containrrr/watchtower
    #     volumes:
    #         - /var/run/docker.sock:/var/run/docker.sock
    # home_assistant:
    #     container_name: home_assistant
    #     image: "ghcr.io/home-assistant/home-assistant:stable"
    #     volumes:
    #         - ./assets/general/home_assistant/config:/config
    #         - /etc/localtime:/etc/localtime:ro
    #         - /run/dbus:/run/dbus:ro
    #     restart: unless-stopped
    #     privileged: true
    #     network_mode: host
    # # guacd
    # guacd:
    #     container_name: guacd_compose
    #     image: guacamole/guacd
    #     networks:
    #         guacnetwork_compose:
    #     restart: always
    #     volumes:
    #         - ./assets/general/guacd/drive:/drive:rw
    #         - ./assets/general/guacd/record:/record:rw
    # # postgres
    # postgres:
    #     container_name: postgres_guacamole_compose
    #     environment:
    #         PGDATA: /var/lib/postgresql/data/guacamole
    #         POSTGRES_DB: guacamole_db
    #         POSTGRES_PASSWORD: "ChooseYourOwnPasswordHere1234"
    #         POSTGRES_USER: guacamole_user
    #     image: postgres:13.4
    #     networks:
    #         guacnetwork_compose:
    #     restart: always
    #     volumes:
    #         - ./assets/general/postgres/init:/docker-entrypoint-initdb.d:ro
    #         - ./assets/general/postgres/data:/var/lib/postgresql/data:rw

    # # guacamole
    # guacamole:
    #     container_name: guacamole_compose
    #     depends_on:
    #         - guacd
    #         - postgres
    #     environment:
    #         GUACD_HOSTNAME: guacd
    #         POSTGRES_DATABASE: guacamole_db
    #         POSTGRES_HOSTNAME: postgres
    #         POSTGRES_PASSWORD: "ChooseYourOwnPasswordHere1234"
    #         POSTGRES_USER: guacamole_user
    #     image: guacamole/guacamole
    #     links:
    #         - guacd
    #     networks:
    #         guacnetwork_compose:
    #     ports:
    #         ## enable next line if not using nginx
    #         - 8090:8080/tcp # Guacamole is on :8090/guacamole, not /.
    #     ## enable next line when using nginx
    #     ##    - 8080/tcp
    #     restart: always
